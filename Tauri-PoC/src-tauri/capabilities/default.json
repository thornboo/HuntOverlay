{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum local capability for the control and overlay PoC windows.",
  "windows": ["control", "overlay"],
  "permissions": ["core:default"]
}
